#  Как работать с репозиторием финального задания

## Что нужно сделать

Настроить запуск проекта Kittygram в контейнерах и CI/CD с помощью GitHub Actions

## Как проверить работу с помощью автотестов

В корне репозитория создайте файл tests.yml со следующим содержимым:
```yaml
repo_owner: ваш_логин_на_гитхабе
kittygram_domain: полная ссылка (https://доменное_имя) на ваш проект Kittygram
taski_domain: полная ссылка (https://доменное_имя) на ваш проект Taski
dockerhub_username: ваш_логин_на_докерхабе
```

Скопируйте содержимое файла `.github/workflows/main.yml` в файл `kittygram_workflow.yml` в корневой директории проекта.

Для локального запуска тестов создайте виртуальное окружение, установите в него зависимости из backend/requirements.txt и запустите в корневой директории проекта `pytest`.

## Чек-лист для проверки перед отправкой задания

- Проект Taski доступен по доменному имени, указанному в `tests.yml`.
- Проект Kittygram доступен по доменному имени, указанному в `tests.yml`.
- Пуш в ветку main запускает тестирование и деплой Kittygram, а после успешного деплоя вам приходит сообщение в телеграм.
- В корне проекта есть файл `kittygram_workflow.yml`.


Проект kittygram - платформа, на которой пользователи могут публиковать посты со своими пушистыми друзьями - котами. В посте можно загрузить фотографию питомца, указать его день
рождения, цвет и достижения. 

Для запуска проекта нужно:
1) Установить докер на свою машину (программа работает на Linux, поэтому для других OC надо использовать виртуализацию или WSL);
2) Создать пустую директорию для проекта;
3) В эту директорию скачать файл docker-compose.production.yml с удаленного репозитория: https://github.com/TimurBikmaev/kittygram_final#;
3) В терминале дистрибутива Linux или WSL перейти в директорию проекта и прописать команду: sudo docker compose -f docker-compose.production.yml -d;
4) Перейти по домену: https://yakittygram.servemp3.com/

Примеры запросов:
1) GET /cats/: список опубликованных котов;
2) GET /cats/{id}/: ифнормация об одном коте;
3) POST /cats/: создать кота;
4) PUT /cats/{id}/: изменить информацию о коте;
5) GET /cats/{id}/achievments: достижения кота.

В проекте используются: django, DRF, djoser, gunicorn, nginx, docker.

Автор: Тимур Бикмаев, студент Я.практикума. 
Контакты: bikma2004@gmail.com



